# Perceptual Importance Adaptive Path Tracing — 개발기획안

**브랜치**: `feature/perceptual-importance-pt`  
**부모**: `feature/pbr-path-tracing`  
**최종 갱신**: 2026-05-26

---

## 1. 연구 목표 & 차별점

### 한 줄 정의
> **인간 시각 attention 기반 ray budget 집중 + temporal artifact는 별도 단계에서 처리**

### 기존 방식과의 차별점

| 측면 | 기존 PT | 본 방식 |
|------|--------|--------|
| Ray budget | 모든 픽셀 균일 | perceptual importance Î에 비례 |
| 디노이즈 | 균일 강도 | Î가 낮을수록 강한 reconstruction |
| Importance 정의 | variance 기반 (artifact 측정) | edge/depth/specular/relative-motion (perceptual attention) |
| Temporal artifact | importance 함수에 섞임 (feedback loop) | **PASS 3 별도 단계에서 처리** |
| Specular 처리 | raw luminance (firefly 폭발) | `(1-r)²·clamp(H, 0, H_99)` |
| Motion 처리 | 절대 motion (카메라 패닝 시 폭증) | **상대적 motion** (`|v - v_neighborhood|`) |
| Semantic 통합 | 선형 합산 (스케일 불일치) | **multiplier `(1 + w_v·V)`** |

### 핵심 수식

```
PASS 1: I(x,y)      = w_e·Ê + w_d·D̂ + w_s·Ŝ + w_m·M̂
        I_final     = I · (1 + w_v · V)
        Ê           = E / (percentile_99(E) + ε)   [정규화]
        Î_smooth(t) = α·Î_raw(t) + (1-α)·Î_smooth(t-1)   [EMA]

PASS 2: R_i(x,y)    = R_min + (R_max - R_min) · Î_i^γ
        R_total_new = R_total · (16ms / T_gpu)            [DFTC]

PASS 3: W_denoise   = 1 - Î                                [adaptive filter]
        + history clamp / firefly rejection / reprojection confidence
```

**가중치 초기값**: `w_e=0.40, w_d=0.25, w_s=0.20, w_m=0.15, γ=1.0`

---

## 2. 전체 파이프라인

```
┌─────────────────────────────────────────────────────────────┐
│ GBuffer Generation (RT primary hit, 1spp)                    │
│   → depth, normal, motionVec, roughness, luminance(prev frame)│
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ PASS 1 — Perceptual Importance Estimation                    │
│   E (edge)  D (depth)  S (specular)  M (rel motion)  V (sem) │
│   percentile_99 정규화 → weighted sum → multiplier V         │
│   Temporal EMA smoothing → Î(x,y) ∈ [0,1]                    │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ PASS 2 — Adaptive Path Tracing                               │
│   per-pixel R_i = lerp(R_min, R_max, Î^γ)                    │
│   Tier 1 (Î>0.7): full PT no-reuse                           │
│   Tier 2 (0.3~0.7): reduced PT + spatial reservoir reuse     │
│   Tier 3 (Î<0.3): minimal PT + aggressive temporal reuse     │
│   Dynamic Frame Time Compensation                            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ PASS 3 — Temporal Stabilization & Perceptual Denoising       │
│   3.1 Temporal Reprojection (MV-based history fetch)         │
│   3.2 History Accumulation + Clamp (Salvi 2016)              │
│   3.3 Firefly / Outlier Rejection                            │
│   3.4 Variance Estimation                                    │
│   3.5 Hole Detection (depth/normal/MV/objID confidence)      │
│   3.6 Importance-weighted Adaptive A-trous (radius ∝ 1-Î)    │
│   3.7 Frequency Fusion (bilateral high/low split)            │
│   3.8 NRD Residual Stabilizer (low strength)                 │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Composite + DLSS-RR (Color = denoised, optional confidence)  │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Phase 분해 (각 ~70% 세션 단위)

### Phase 1 — Importance Map 인프라 (E + D 최소 동작)

**목표**: percentile_99 정규화 없이 raw E + D만으로 동작하는 importance map 생성 + 시각화

**산출물**:
- `shaders/ImportanceMap.hlsl` — compute shader
  - 입력: `g_depth` (R32F), `g_accumulation` (RGBA32F luminance)
  - 출력: `m_importanceMap` (R16F, render-res)
  - Sobel 3×3 on depth → D
  - Sobel 3×3 on luminance → E
  - `I = 0.5·E + 0.5·D` (단순 합)
- `src/ImportanceMapPass.{h,cpp}` — Denoiser/SVGF 패턴 따라 wrapper class
- App.{h,cpp} 통합: BuildDescriptors에 슬롯 추가, Render 루프에 dispatch 추가
- 디버그 모드 확장 (V 키): `debugMode=10` → importance map 시각화 (grayscale)

**검증**:
- 빌드 OK
- V 키로 디버그 모드 진입 시 화면에 흑백 importance 표시
- 깊이 경계 + 색상 경계 위치에 흰색 픽셀이 보여야 함

**의존성**: 없음 (현재 NRD 통합 미완 상태에서도 진행 가능)

---

### Phase 2 — Importance Map 정규화 + S/M/V 항 완성

**목표**: percentile_99 정규화 + S/M/V 추가 + 가중치 합

**산출물**:
- `shaders/ComputePercentile99.hlsl` — histogram 기반 percentile 계산
  - 256-bin histogram (compute shader 1패스)
  - prefix sum → 99% 위치의 bin 찾기 (compute shader 2패스)
  - `m_percentile99` (R32F, 1×1 텍스처) 출력
  - E, D, S 각각에 대해 별도 계산 (또는 단일 결합 metric)
- `ImportanceMap.hlsl` 확장:
  - S 항: `(1-r)² · clamp(H, 0, H_99)`  
    H = primary hit의 specular luminance
  - M 항: `|v(x,y) - mean(v in 3×3)|` (상대적 motion)
  - V 항: stub (R8 텍스처, 현재는 0으로 채움 — Phase 4 이후 semantic NN 연결)
  - 가중치: `I = 0.40·Ê + 0.25·D̂ + 0.20·Ŝ + 0.15·M̂`
  - `I_final = I · (1 + w_v · V)`
- `cb.importanceWeights[5]` constant buffer field 추가 (런타임 ablation용)

**검증**:
- specular highlight 위치 (메탈 큐브 표면)에서 importance 상승
- 카메라 패닝 시 화면 전체 importance 폭증 없음 (M의 상대적 모션 효과)
- firefly 픽셀이 importance를 폭주시키지 않음 (S clamp 효과)

**의존성**: Phase 1

---

### Phase 3 — Temporal EMA Smoothing + History Buffer

**목표**: importance map 안정화 + motion-aware reset

**산출물**:
- `m_importanceHistory` 버퍼 (R16F, render-res)
- `shaders/ImportanceEMA.hlsl` — compute shader
  ```hlsl
  float Imv = length(motionVec);
  float alpha = (Imv > motionThreshold) ? 1.0 : g_alpha;  // 0.1~0.3
  Î_smooth = lerp(Î_history, Î_raw, alpha);
  ```
- App에서 매 프레임: `Î_history ← Î_smooth` (Copy 또는 ping-pong)
- 정규화는 EMA **전에** 적용 (수식 순서 명확화)

**검증**:
- 정적 씬에서 importance가 시간적으로 안정 (frame-to-frame 차이 < 5%)
- 빠른 카메라 회전 시 motion gate가 발동되어 history 리셋

**의존성**: Phase 2

---

### Phase 4 — Adaptive Ray Allocation (PASS 2 기본)

**목표**: per-pixel `R_i`에 따른 maxBounce 동적 결정

**산출물**:
- `shaders/Raytracing.hlsl` 수정:
  - SRV 추가: `t6 g_importanceMap`
  - `cb.rMin = 1, cb.rMax = 8, cb.gamma = 1.0` 추가
  - RayGen 시작:
    ```hlsl
    float I = g_importanceMap.SampleLevel(s_clamp, uv, 0);
    uint maxBounce = uint(lerp(cb.rMin, cb.rMax, pow(I, cb.gamma)));
    ```
  - 기존 `k_maxBounce` 상수를 `maxBounce` 변수로 교체
- 루트 시그니처 업데이트: SRV range 6개 (TLAS + 4 VB + importance)
- 디버그 모드 확장: `debugMode=11` → bounce count heatmap

**검증**:
- importance 높은 영역(specular/edge) bounce 8회까지 → noise ↓
- importance 낮은 영역(uniform diffuse wall) bounce 1~2회 → 빠름
- 전체 GPU 시간 측정 (`T 키`) — 균일 8spp 대비 비슷하거나 감소

**의존성**: Phase 3 (안정화된 Î 필요)

---

### Phase 5 — Dynamic Frame Time Compensation (PASS 2 완성)

**목표**: 프레임 타임에 따라 ray budget 자동 조절

**산출물**:
- App.cpp: GPU timestamp query (이미 부분 구현)
- 매 프레임:
  ```cpp
  float T_gpu_ms = MeasureGPUTime();
  float compensation = 16.0f / max(T_gpu_ms, 1.0f);
  cb.rMax = clamp(baseRMax * compensation, 2, 8);
  ```
- EMA로 compensation 안정화 (한 프레임 spike에 과민반응 방지)
- 콘솔 출력: 매 60프레임마다 평균 GPU ms + 현재 R_max

**검증**:
- 복잡한 씬 진입 시 R_max 감소 (예: 8 → 5) — 프레임 유지
- 단순 씬으로 돌아가면 R_max 회복

**의존성**: Phase 4

---

### Phase 6 — Tier 기반 분기 (PHTR 통합)

**목표**: Î threshold로 Tier 1/2/3 분류 + Tier별 reuse 정책

**산출물**:
- `Raytracing.hlsl`:
  - `tier = (I > 0.7) ? 1 : (I > 0.3) ? 2 : 3`
  - Tier 1: 기존 그대로
  - Tier 2/3: reservoir 읽기 활성화 (다음 Phase 7에서 사용할 자리)
  - 현재 Phase 6에서는 **분기만 표시** (실제 reservoir 미사용)
- `cb.tierThresholds = {0.3, 0.7}` 추가
- 디버그 모드 확장: `debugMode=12` → Tier 시각화 (RGB로 1=빨강, 2=초록, 3=파랑)

**검증**:
- 씬 3(PBR showcase)에서 spec 구 = Tier 1(빨강), 흰 벽 = Tier 3(파랑)
- threshold 슬라이더(키보드 단축키)로 분포 변화 확인

**의존성**: Phase 4

---

### Phase 7 — Spatial Reservoir Reuse (Tier 2/3)

**목표**: BRDF/light sample을 reservoir로 reuse — radiance 결과는 reuse 안 함

**산출물**:
- `m_reservoir` 버퍼 (RGBA32U, render-res):
  - x,y: scatter direction (oct-packed)
  - z: pdf
  - w: M count + flag
- `Raytracing.hlsl`:
  - Tier 1: reservoir write only (다음 프레임 위해)
  - Tier 2: 3×3 neighbor 중 1개 reservoir 무작위 선택 + RIS resampling
  - Tier 3: 5×5 neighbor + 더 강한 reuse
- 첫 bounce direction만 reuse, 이후 bounce는 자체 sampling

**검증**:
- Tier 3 영역에서 분산 ↓ (frame-to-frame 변화 측정)
- Tier 1 영역(specular)은 그대로 (radiance ghosting 없음)
- Bitterli 2020 ReSTIR 단순화 버전이므로 ghost 위험 낮음

**의존성**: Phase 6

---

### Phase 8 — Temporal Reprojection + History Clamp (PASS 3.1~3.2)

**목표**: motion-vector 기반 history fetch + Salvi clamp

**산출물**:
- `m_colorHistory` 버퍼 (RGBA16F, render-res)
- `shaders/TemporalReproject.hlsl` — compute shader
  - prev UV = curr UV + MV (이미 RT에서 계산)
  - bilinear sample from `m_colorHistory`
  - reprojection confidence (depth + normal + objectID 일치)
  - YCoCg color space로 변환
  - 3×3 neighborhood min/max bounding box
  - history clamp: `clamp(history, neighborMin, neighborMax)`
  - history accumulation: `out = lerp(history_clamped, current, 1/(N+1))` (N capped at 64)
- 출력: `m_colorStabilized`

**검증**:
- specular highlight ghosting 최소화 (clamp 효과)
- 정적 씬에서 점진적 수렴 (LPIPS 감소)
- 빠른 회전 시 confidence reject → 즉시 current frame

**의존성**: Phase 4 (PT 결과가 있어야), Phase 1 (depth/normal 필요)

---

### Phase 9 — Hole Detection + Firefly Rejection (PASS 3.3~3.5)

**목표**: 4-조건 hole mask + outlier 제거

**산출물**:
- **Object ID 추가**: `Raytracing.hlsl`에 instance ID GBuffer 출력 (uint, R8_UINT or pack into existing buffer)
- `m_holeMask` (R8_UNORM, render-res)
- `shaders/HoleDetection.hlsl`:
  ```hlsl
  bool hole = |z_curr - z_reproj| > 0.05·z_curr
           || dot(n_curr, n_reproj) < 0.9
           || length(mv - mv_neighborhood_mean) > 2.0
           || objID_curr != objID_reproj;
  ```
- `shaders/FireflyReject.hlsl`:
  - 3×3 neighborhood luminance median
  - 현재 luminance > 4× median → outlier
  - outlier 시 neighborhood mean으로 대체
- 출력: hole-marked + firefly-rejected radiance

**검증**:
- 카메라 회전 시 hole 픽셀이 silhouette 따라 표시 (디버그 시각화)
- firefly 빈도 ↓ (per-frame max luminance 측정)

**의존성**: Phase 8

---

### Phase 10 — Importance-Weighted Adaptive A-trous (PASS 3.6)

**목표**: 기존 A-trous를 Î와 hole-mask 입력받게 수정

**산출물**:
- `shaders/Denoise.hlsl` 수정:
  - SRV 추가: `t3 g_importanceMap`, `t4 g_holeMask`
  - per-pixel radius: `radius = lerp(2, 5, 1.0 - Î)` (Î 낮을수록 wide)
  - sigma_color도 Î에 따라: `sigma = baseSigma * (1.5 - Î)`
  - hole==1 픽셀: 항상 5-radius (강한 reconstruction)
  - hole==0 픽셀 + Î > 0.8: passthrough (skip filter)
- 3-pass 그대로 유지, 마지막 패스는 tonemap

**검증**:
- spec 영역 detail 보존 (Î=0.9 → passthrough)
- diffuse 영역 강한 smoothing (Î=0.1 → 5×5 강한 필터)
- hole 영역 깨끗하게 reconstruction

**의존성**: Phase 9 + Phase 3 (Î 안정화 필요)

---

### Phase 11 — Frequency Fusion (PASS 3.7, PHTR Bilateral)

**목표**: high-freq current 보존 + low-freq history reuse

**산출물**:
- `shaders/FrequencyFusion.hlsl` (2 dispatch):
  - Pass A — bilateral low-pass:
    ```hlsl
    L_low_curr = bilateral_blur(L_input, radius=5, σ_spatial=2.0, σ_range=adaptive)
    ```
  - Pass B — fusion:
    ```hlsl
    L_high = L_input - L_low_curr;
    L_low_stable = lerp(L_low_history, L_low_curr, 0.1);  // 강한 reuse
    L_final = L_low_stable + L_high;
    L_low_history ← L_low_stable;
    ```
- `m_lowFreqHistory` 버퍼 추가
- A-trous 출력을 입력으로

**검증**:
- 정적 씬에서 diffuse 영역 수렴 가속 (low-freq EMA)
- 카메라 회전 시 edge 선명도 유지 (high-freq current)

**의존성**: Phase 10

---

### Phase 12 — NRD Residual Stabilizer 배치

**목표**: 기존 NRDDenoiser를 residual 위치로 옮기고 약한 설정

**산출물**:
- `NRDDenoiser::Denoise` 호출을 frequency-fused 결과에 대해
- `RelaxSettings` 변경:
  - `diffuseMaxAccumulatedFrameNum = 4` (기본 32 → 약하게)
  - `specularMaxAccumulatedFrameNum = 8`
  - `enableAntiFirefly = false` (이미 Phase 9에서 처리)
- NRD 입력 normal+roughness encoding 변환 패스도 이 Phase에서 완성
  - `shaders/PackNormalRoughness.hlsl` — R10G10B10A2 oct-pack

**검증**:
- 미세 shimmer 잔여물 추가 안정화
- ghosting 증가하지 않음 (약한 설정 덕분)
- 전체 파이프라인 GPU 시간 < 16ms (60fps 유지)

**의존성**: Phase 11 + 기존 NRD Init (현재 코드 상태)

---

### Phase 13 — DLSS-RR Composite 연결

**목표**: NRD 출력을 DLSS-RR Color 입력으로 연결 + composite

**산출물**:
- `shaders/Composite.hlsl` — denoised diff/spec × albedo 합성
  ```hlsl
  finalColor = diffDenoised * diffAlbedo
             + specDenoised * specAlbedo
             + emission;
  ```
- DLSS-RR `m_renderAccum` 입력을 composite 결과로 교체
- DLSS-RR confidence input (선택): `IN_DIFF_CONFIDENCE = Î`, `IN_SPEC_CONFIDENCE = Î_spec`

**검증**:
- 최종 화면이 PHTR pipeline 결과로 출력됨
- 기존 DLSS-RR 단독 대비 paper-crumpled 노이즈 제거 확인
- U 키 토글로 ON/OFF 비교 가능

**의존성**: Phase 12

---

### Phase 14 — Metrics 수집 + 디버그 UI

**목표**: LPIPS / Temporal Stability / SSIM 자동 수집

**산출물**:
- `src/MetricsCollector.{h,cpp}`:
  - LPIPS: Python 헬퍼 (off-line, 저장된 reference vs current 비교)
  - Temporal Stability: `mean(|frame_t - frame_{t-1}|)` GPU compute
  - SSIM: 간단한 11×11 Gaussian window 구현
- 키바인딩: `T` (트레이스 시작/종료), `Y` (마지막 N프레임 평균±σ 출력)
- CSV 출력: `metrics_<sceneID>_<config>.csv`

**검증**:
- 3개 씬 × 3개 구성(uniform 1spp, variance, ours) × 100프레임 평균 결과
- LPIPS reference: 1000spp offline render (별도 1회 생성)

**의존성**: Phase 13

---

### Phase 15 — Ablation Study 자동화

**목표**: 가중치 / γ / threshold ablation 표 자동 생성

**산출물**:
- 키 바인딩: `Shift+1~9` → 가중치 프리셋 전환
- Ablation 모드: `M 키` → 자동으로 9개 조합 ×N프레임 측정 후 CSV 저장
- 조합:
  - 가중치: `{w_e: 0.2/0.4/0.6}` × `{w_d: 0.1/0.25/0.4}`
  - γ: `{1.0, 1.5, 2.0, 3.0}`
  - T항 ON/OFF (검증: 본 구조에서 OFF가 더 좋아야 함)
- 결과 표 자동 생성 스크립트: `scripts/ablation_table.py`

**검증**:
- 9~12개 셀의 결과 표 CSV 출력
- 우리 기본 설정이 다른 ablation보다 LPIPS 우수해야 함

**의존성**: Phase 14

---

### Phase 16 — User Study 자료 준비

**목표**: 2AFC 비교 영상 + 정적 이미지 세트

**산출물**:
- `scripts/record_session.py` — 카메라 경로 ↔ 5초 영상 자동 녹화
- 3씬 × 3구성 × 2초/씬 → MP4 출력
- 정적 비교 이미지: 각 씬에서 critical view 5개
- Google Forms 템플릿 (별도 문서)

**검증**:
- 녹화 영상 framerate stable (30fps)
- 같은 카메라 경로에서 method만 다름

**의존성**: Phase 15

---

## 4. Phase 의존 그래프

```
Phase 1 (E+D)
  └─→ Phase 2 (정규화+S+M+V)
        └─→ Phase 3 (EMA)
              ├─→ Phase 4 (Adaptive Ray)
              │     ├─→ Phase 5 (DFTC)
              │     └─→ Phase 6 (Tier 분류)
              │           └─→ Phase 7 (Reservoir Reuse)
              └─→ Phase 10 (Adaptive A-trous, Î 사용)
              
Phase 4 ─→ Phase 8 (Temporal Reprojection)
              └─→ Phase 9 (Hole + Firefly)
                    └─→ Phase 10 (Adaptive A-trous)
                          └─→ Phase 11 (Frequency Fusion)
                                └─→ Phase 12 (NRD Residual)
                                      └─→ Phase 13 (DLSS Composite)
                                            └─→ Phase 14 (Metrics)
                                                  └─→ Phase 15 (Ablation)
                                                        └─→ Phase 16 (User Study)
```

총 16 Phase. 1 Phase = 1 세션의 ~70%. 빌드 검증 + commit + 다음 세션 인계 메모(memory 파일 갱신) 시간이 나머지 30%.

---

## 5. 핵심 결정 사항 (기록)

### T항 분리 원칙 (Notion 문서 7장)
> Importance function은 **순수 perceptual attention**만 담당.  
> Temporal instability(`|C_t - C_{t-1}|`)는 artifact 측정값이므로 PASS 3에서 처리.  
> → feedback loop, camera motion 과민반응, noise 자체에 민감해지는 문제 회피.

### Percentile_99 vs max() 정규화
> max() 사용 시 firefly 단일 픽셀이 전체 스케일을 왜곡.  
> percentile_99 사용 → outlier robust.  
> Reinhard 2002 톤매핑에서 검증된 기법.

### V항 multiplier 처리
> Semantic saliency는 다른 항들과 스케일/의미가 다름.  
> 선형 합산 대신 `I·(1 + w_v·V)` multiplier로 처리.  
> V=0이면 I_final = I (semantic 없을 때 영향 없음).

### Specular `(1-r)²·clamp(H, 0, H_99)`
> raw H 사용 시 firefly에서 폭주.  
> H_99 clamp로 stable.  
> `(1-r)²`는 roughness가 낮을수록 importance 증가 (사람이 specular highlight에 집중).

### 상대적 motion `|v - v_mean_neighborhood|`
> 절대 `sqrt(vx²+vy²)` 사용 시 카메라 패닝이 화면 전체 importance 폭증시킴.  
> 주변 대비 튀는 motion만 의미 있음.

### 가중치 초기값
> `w_e=0.40, w_d=0.25, w_s=0.20, w_m=0.15`  
> Daly 1993 HVS CSF + Mannos & Sakrison 1974 근거.  
> Phase 15 ablation으로 검증/조정.

---

## 6. 측정 지표 우선순위

| 지표 | 역할 | 비고 |
|------|------|------|
| **LPIPS** | Primary | Zhang 2018 — perceptual quality |
| Temporal Stability | 핵심 보조 | flickering 측정 (Salvi 2016 컨벤션) |
| GPU ms | 성능 검증 | 16ms target (60fps) |
| SSIM | 보조 | 구조적 유사도 |
| PSNR | 참고용 | HVS와 상관 낮음 (Zhang 2018) |
| User Study | 체감 검증 | 2AFC 평가 (n≥30 권장) |

---

## 7. 비교 baseline

| Method | 설명 | 위치 |
|--------|------|------|
| **Uniform 1 spp** | 모든 픽셀 동일 PT 1spp | 동일 코드, importance=1.0 고정 |
| **Variance Adaptive** | variance 기반 ray 분배 (기존 기법) | 별도 분기 (Phase 14에서 토글) |
| **Perceptual Adaptive (Ours)** | 본 구조 전체 | 기본 |
| Pure DLSS-RR | path tracer 1spp + DLSS-RR만 | 별도 토글 (master branch 비교) |
| NRD-only | PT + NRD denoise (no importance) | 별도 토글 |

---

## 8. 참고 문헌 (출처 표시 필수)

- Itti, Koch & Niebur (1998) — Saliency-Based Visual Attention
- Daly (1993) — Visible Differences Predictor (HVS CSF)
- Mannos & Sakrison (1974) — Visual Fidelity Criterion
- Reinhard et al. (2002) — Photographic Tone Reproduction (percentile)
- Salvi (2016) — Temporal Supersampling (history clamp)
- Bitterli et al. (2020) — ReSTIR (reservoir reuse)
- Schied et al. (2017) — SVGF
- Hasselgren et al. (2020) — NTASD (temporal adaptive sampling)
- Zhang et al. (2018) — LPIPS
- Rousselle et al. (2012) — Adaptive Rendering w/ Weighted Local Regression
- Yarbus (1967) — Eye Movements and Vision

---

## 9. 위험 요소 & 완화 방안

| 위험 | 영향 | 완화 |
|------|------|------|
| Tier 1 영역이 너무 커서 budget 폭주 | 60fps 미달 | Phase 5 DFTC가 R_total 자동 감소 |
| Temporal reuse → ghosting | Tier 1 영역 끈적함 | Phase 8 history clamp + Tier 1 reuse 금지 |
| Reservoir 구현 복잡도 | 일정 지연 | Phase 7 단순 1-sample reservoir로 시작, ReSTIR full은 차후 |
| Percentile_99 계산 비용 | per-frame overhead | 256-bin histogram 1-pass, ~0.2ms 예상 |
| NRD 통합 미완 상태에서 진행 | 의존성 충돌 | Phase 12에서 NRD wiring 마무리. 그 전엔 NRD off로 진행 가능 |
| Semantic NN (V) cost | 별도 추론 부담 | V는 stub으로 시작. Phase 16 이후 선택적 추가 |

---

## 10. 산출물 체크리스트 (졸업 작품 기준)

- [ ] Phase 1~13 모두 구현 (paper의 PASS 1/2/3 완성)
- [ ] Phase 14 metrics 수집 시스템 동작
- [ ] Phase 15 ablation 표 (CSV) 생성됨
- [ ] Phase 16 사용자 평가 영상 + Forms 준비
- [ ] LPIPS 결과 표 (3씬 × 3method × 평균±σ)
- [ ] Temporal Stability 표
- [ ] GPU ms 표 (각 PASS별 + 전체)
- [ ] Figure 1: teaser (Tier 시각화 + 결과)
- [ ] Figure 2: Pipeline 다이어그램
- [ ] Figure 3: 비교 (uniform / variance / ours)
- [ ] Figure 4: GPU 타이밍 bar chart
- [ ] Figure 5: User study 결과
- [ ] 논문 본문 (10~15p) — 이 기획안의 §1, 2를 introduction/method로 확장

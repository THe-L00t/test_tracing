# Perceptual Importance Adaptive Path Tracing — 개발기획안

**브랜치**: `feature/perceptual-importance-pt`  
**부모**: `feature/pbr-path-tracing`  
**최종 갱신**: 2026-05-26

---

## 1. 연구 목표 & 차별점

### 한 줄 정의
> **인간 시각 attention 기반 ray budget 집중 + temporal artifact는 별도 단계에서 처리**

### 기존 방식과의 차별점

| 측면 | 기존 PT | 본 방식 |
|------|--------|--------|
| Ray budget | 모든 픽셀 균일 | perceptual importance Î에 비례 |
| 디노이즈 | 균일 강도 | Î가 낮을수록 강한 reconstruction |
| Importance 정의 | variance 기반 (artifact 측정) | edge/depth/specular/relative-motion (perceptual attention) |
| Temporal artifact | importance 함수에 섞임 (feedback loop) | **PASS 3 별도 단계에서 처리** |
| Specular 처리 | raw luminance (firefly 폭발) | `(1-r)²·clamp(H, 0, H_99)` |
| Motion 처리 | 절대 motion (카메라 패닝 시 폭증) | **상대적 motion** (`|v - v_neighborhood|`) |
| Semantic 통합 | 선형 합산 (스케일 불일치) | **multiplier `(1 + w_v·V)`** |

### 핵심 수식

```
PASS 1: I(x,y)      = w_e·Ê + w_d·D̂ + w_s·Ŝ + w_m·M̂
        I_final     = I · (1 + w_v · V)
        Ê           = E / (percentile_99(E) + ε)   [정규화]
        Î_smooth(t) = α·Î_raw(t) + (1-α)·Î_smooth(t-1)   [EMA]

PASS 2: R_i(x,y)    = R_min + (R_max - R_min) · Î_i^γ
        R_total_new = R_total · (16ms / T_gpu)            [DFTC]

PASS 3: per-pixel exclusive routing
        Tier 1 → ptRadiance
        Tier 2 + mv valid → bilinear sample(prev frame, idx - mv) → Salvi clamp
        else (Tier 3 + invalid T2 + firefly outlier) → A-trous fill → NRD denoise
```

**가중치 초기값**: `w_e=0.40, w_d=0.25, w_s=0.20, w_m=0.15, γ=1.0`

---

## 2. 전체 파이프라인

```
┌─────────────────────────────────────────────────────────────┐
│ GBuffer Generation (RT primary hit, 1spp)                    │
│   → depth, normal, motionVec, roughness, luminance(prev frame)│
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ PASS 1 — Perceptual Importance Estimation                    │
│   E (edge)  D (depth)  S (specular)  M (rel motion)  V (sem) │
│   percentile_99 정규화 → weighted sum → multiplier V         │
│   Temporal EMA smoothing → Î(x,y) ∈ [0,1]                    │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌────────────────────────────┐     ┌──────────────────────────┐
│ PASS 2-A — Surface PT       │     │ PASS 2-B — Volumetric    │
│   per-pixel R_i, Tier 분기  │     │   homogeneous fog        │
│   (Tier 1 = full PT)        │     │   N=32 stochastic march  │
│   Dynamic Frame Time Comp.  │     │   per-light in-scatter   │
│                             │     │   HG phase function      │
└────────────────────────────┘     └──────────────────────────┘
        │                                       │
        ▼                                       ▼
┌────────────────────────────────┐  ┌──────────────────────────┐
│ PASS 3 — HPAR Per-tier Routing │  │ PASS 3-B — Vol Long Reuse│
│   ┌──────────────────────────┐ │  │   reproject + EMA(N≤128) │
│   │ Tier 2: mv reuse (P7)    │ │  │   relaxed confidence     │
│   │   → clamp (P8)           │ │  │   (depth ±15%, no normal)│
│   └──────────────────────────┘ │  │                          │
│   ┌──────────────────────────┐ │  └──────────────────────────┘
│   │ Hole mask (P9):          │ │
│   │   Tier 3 + invalid T2 +  │ │
│   │   firefly outlier        │ │
│   └──────────────────────────┘ │
│   ┌──────────────────────────┐ │
│   │ A-trous hole-fill (P10): │ │
│   │   hole → valid neighbor  │ │
│   │   (write only to holes)  │ │
│   └──────────────────────────┘ │
│   ┌──────────────────────────┐ │
│   │ NRD denoise (P12-A)      │ │
│   │   on filled radiance     │ │
│   └──────────────────────────┘ │
│   ┌──────────────────────────┐ │
│   │ Per-tier Composite (P12B)│ │
│   │   Tier 1 → PT raw        │ │
│   │   Tier 2 valid → reused  │ │
│   │   else → NRD output      │ │
│   │   ※ exclusivity 강제     │ │
│   └──────────────────────────┘ │
└────────────────────────────────┘
        │                                       │
        └───────────────┬───────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Volumetric composite                                         │
│   final = surface * vol.transmittance + vol.inscatter        │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ DLSS-RR (P13, 디노이저 아닌 업스케일러로 취급, 모든 픽셀 통과)│
└─────────────────────────────────────────────────────────────┘
```

**Exclusivity 원칙** (PASS 3 의 핵심):
- 한 픽셀에 대해 PT / reuse / denoise 중 **정확히 하나의 출력** 만 최종 값으로 사용
- ray-shot 픽셀은 reuse/denoise 영향 절대 안 받음
- Composite 단계가 final enforcement 지점

---

## 3. Phase 분해 (각 ~70% 세션 단위)

### Phase 1 ✅ COMPLETED — Importance Map 인프라 (E + D 최소 동작)

**완료 commit**: `5b04b7b` (인프라) → `470c476` (F1 시각화) → `3fcdf80` (E 노이즈 fix) → `f8b9d52` (normal-based 안쪽 모서리) → (Phase 1 마감 cleanup)

**최종 산출물**:
- `shaders/ImportanceMap.hlsl` — compute shader
  - 입력: `g_depth` (R32F, t0), `g_accumulation` (RGBA32F, t1), `g_normals` (RGBA16F world-normal+roughness, t2)
  - 출력: `m_importanceMap` (R16F, render-res)
  - `E = Sobel(log(1+L))` — log-compressed luminance edge (1spp 노이즈 영향 ↓)
  - `Dz = Sobel(depth)` — 외부 모서리 (occlusion)
  - `Dn = max_{neighbors} (1 - dot(n_c, n_s))` — 내부 모서리 (concave)
  - `I = w_e·E + w_d·max(Dz, Dn)`, w_e=0.2 / w_d=0.8 (Phase 3 EMA 후 0.40/0.25 복원)
- `shaders/ImportanceVisualize.hlsl` — R16F → display-res RGBA8 heatmap (검정→노랑→흰색)
- `src/ImportanceMapPass.{h,cpp}` — 자체 root sig + PSO + descriptor heap (SRV×3 + UAV×1)
- `src/ImportanceVisualizePass.{h,cpp}` — 시각화 패스 (RGBA8 display-res 출력)
- `src/ShaderCompile.h` — 공통 DXC 컴파일 헬퍼 (HPAR-PT 새 패스 전용)
- App.{h,cpp} 통합: DLSS Init 직후 두 패스 Init, F1 키 토글, 렌더 루프 분기 (debug ON → DLSS Evaluate 우회)

**검증 결과**:
- ✅ 빌드 OK
- ✅ F1 키로 heatmap 진입 — 검정/노랑/흰색 grayscale
- ✅ 외부 모서리 (silhouette, 큐브 외곽) → 노랑/흰색
- ✅ 내부 모서리 (벽-바닥 경계, 방 코너) → 노랑 (Dn 항 추가로 검출됨)
- ✅ flat 벽 가운데 → 검정 (log 압축으로 노이즈 영향 ↓)
- ⚠️ specular highlight 위치 → 미검출 (Phase 2 S 항에서 추가)

**의존성**: 없음. NRD 통합 미완 상태에서 독립 진행됨.

**Phase 1 정리 사항**:
- 5개 commit 으로 분할 진행 (인프라 → 시각화 → E 노이즈 fix → normal-based 안쪽 모서리 → 정리)
- 공통 DXC 컴파일 헬퍼 (`src/ShaderCompile.h`) 추출, HPAR-PT 새 compute 패스 모두 공유
- Phase 1 임시 스케일/가중치는 `Phase 2 에서 percentile_99 로 교체` 주석 명시

---

### Phase 2 ✅ COMPLETED — Importance Map 정규화 + S/M/V 항 완성

**완료 commit**: `660e297` (3-pass 파이프라인) → `d1b74d8` (Sobel boundary OOB fix) → `8cf280a` (F2 진단 + ablation 도구)

**최종 산출물**:
- `shaders/MetricHistogram.hlsl` — 256-bin × 3 metric atomic 히스토그램 구축 (E, S, M)
  - `D` 는 이미 `max(Dz_n, Dn)` 으로 [0,1] bounded 라 percentile 불필요
- `shaders/MetricReduce.hlsl` — 단일 스레드 sequential scan → percentile_99 (1×float4)
  - 768 op, µs 단위. 병렬 prefix sum 대비 단순함 우선
- `ImportanceMap.hlsl` 완성형:
  - E = Sobel(log(1+L))
  - D = max(Sobel(depth)·50, NormalEdgeMax)  ← outer + inner 모서리
  - S = (1-r)² · |F0| · log(L)
  - M = |v - mean(v in 3×3)|
  - V = 0 (Phase 16 stub)
  - X̂ = X / (percentile_99 + ε) for X in {E, S, M}
  - I = 0.40·Ê + 0.25·D̂ + 0.20·Ŝ + 0.15·M̂
  - I_final = I · (1 + w_v · V)
- `ImportanceMapPass` 3-PSO 오케스트레이션:
  - root sig × 3 (Histogram/Reduce/Importance)
  - descriptor heap 9 슬롯 (SRV×6 + UAV×3)
  - ClearUnorderedAccessViewUint 용 non-visible heap
  - barriers (UAV barrier histogram, UAV→SRV percentile)
- 진단/ablation 도구: **F2 cycling** (All → E → D → S → M → All)
  - Phase 15 ablation 자동화 시 그대로 재활용

**검증 결과**:
- ✅ specular highlight (메탈 큐브) → 노랑/흰색 (S 항 효과)
- ✅ 카메라 패닝 시 화면 전체 폭증 없음 (M 의 상대적 모션 효과)
- ✅ firefly outlier → percentile_99 robust 흡수
- ✅ sphere-floor silhouette (D + M) 명확히 검출 — 의도된 perceptual edge
- ✅ Sobel boundary 가짜 edge (display 경계 가로선) 제거 — ClampPx 도입

**의존성**: Phase 1 (E + D 기반)

**Phase 2 정리 사항**:
- 3개 commit (인프라 + boundary fix + 진단도구)
- 히스토그램 접근 형식: `D = max(Dz_n, Dn)` 이미 bounded 라 percentile 비대상 (E/S/M 만)
- F2 진단은 Phase 15 ablation 단계로 자연 승계

---

### Phase 3 ✅ COMPLETED — Temporal EMA Smoothing + History Buffer

**완료 commit**: `ae0d7bf` (EMA 4-pass + ping-pong) → (Phase 3 마감 cleanup)

**최종 산출물**:
- `shaders/ImportanceEMA.hlsl` — α blending + motion gate + first-frame reset
  - `Î_smooth = lerp(history, raw, α)`, α=0.2 기본
  - `|MV| > 2.0px → α=1.0` (history reject)
  - `firstFrame → α=1.0` (씬 전환 / Init)
- `ImportanceMapPass` 4-PSO 확장 (Histogram → Reduce → ImportanceMap → EMA)
- ping-pong `m_smooth[2]` (R16F render-res), `m_writeIdx` 토글
- `Resource()` 의미 변경 — 가장 최근 작성된 smooth 버퍼 반환
- `ResetHistory()` 공개 메서드 → `App::SwitchScene` 에서 호출
- 힙 13 슬롯 확장 (Phase 2 9 → 4 슬롯 추가)
- V (semantic saliency) 텍스처는 Phase 16 자리 — 현재 셰이더 상수 V=0

**검증 결과**:
- ✅ 정적 씬: D-only/M-only 모드 완벽 안정. E/S 항은 1spp PT noise 33% 잔존 (이론적 한계 = `σ²·α/(2-α)`)
- ✅ 빠른 회전: motion gate 발동 → 즉시 반응, 잔상 없음
- ✅ 씬 전환: history reset → 1프레임 새 분포
- ✅ F2 단독 모드도 EMA 통과 (M-only 페이드 아웃 확인)
- ✅ 상대적 모션 정의 정확 동작: translation 시만 모서리 띠, rotation 시 없음
- ⚠️ 1spp PT 본질 noise → E/S 항 잔존. Phase 12 NRD residual 의 prefilter 입력으로 해결 예정

**의존성**: Phase 2

**Phase 3 정리 사항**:
- F1 디버그는 internal data 시각화 — **최종 렌더 픽셀에 importance 안 보임**
- Phase 4 부터 importance 가 ray allocation 에 적용되지만 visible ghosting 없음 (간접적 quality 향상)
- V semantic 텍스처는 Phase 16 NN 통합 시 추가 — 현재 셰이더 V=0 상수로 처리

---


### Phase 4 ✅ COMPLETED — Adaptive Ray Allocation (PASS 2 기본)

**완료 commit**: `e749bcb` (R_i = lerp(R_min, R_max, Î^γ)) → `81fb0c1` (R_min 1→4 fix)

**최종 산출물**:
- `src/Common.h`: SceneCB 320→336 bytes
  - `adaptiveRayEnabled` (replaces `_pad1`)
  - `rMin, rMax, gamma, _pad2`
- `shaders/Raytracing.hlsl`:
  - SRV `t5 g_importance` (R16F, Phase 3 smooth) 추가
  - CB 매칭 필드 추가
  - RayGen 시작에 per-pixel maxBounce 계산:
    ```hlsl
    if (adaptiveRayEnabled) {
        float I = g_importance.Load(int3(idx, 0));
        float R = lerp(rMin, rMax, pow(saturate(I), gamma));
        maxBounce = clamp(round(R), 1, k_maxBounce);
    }
    ```
  - `for (bounce < k_maxBounce)` → `for (bounce < maxBounce)`
- `src/DXRPipeline.cpp`: root sig SRV 5→6 (t0..t5)
- `src/App.{h,cpp}`:
  - `m_importanceSrvNonDLSS/m_importanceSrvDLSS` CPU 핸들
  - 슬롯 15 = SRV t5 importance (비DLSS), 매 frame ping-pong 갱신
  - DLSS 진입 디스크립터 베이스 15→16
  - `UpdateSceneCB`: adaptiveRayEnabled + rMin=4 + rMax=8 + γ=1.0
- `src/DLSSIntegration.{h,cpp}`:
  - sphere VB 미러 다음 슬롯에 importance SRV 추가 (slot 31)
  - `ImportanceMirrorCPU()` 접근자
  - 슬롯 주석 16~31 로 shift 갱신
- 힙 layout 주석 갱신 (App.cpp + DLSSIntegration.h)

**파라미터 (Phase 4 베이스라인)**:
- `R_min=4, R_max=8, γ=1.0` — 4~8 사이 bounce 분배
- Phase 7 (spatial reservoir reuse) 후 R_min=1~2 로 다시 낮춤 가능

**검증 결과**:
- ✅ 빌드 OK
- ✅ DLSS ON: edge/specular 영역 8 bounce 유지, flat 영역 4 bounce 로 분배
- ✅ 유리/반투명 정상 렌더 (4 bounce 로 refraction 도달)
- ✅ flat 벽 firefly noise 미발생 (R_min=1 시점 vs 4 비교 검증됨)
- ✅ 비DLSS 모드 무영향 (adaptiveRayEnabled=0)
- ⚠️ 카메라 motion 시 지글거림 잔존 — DLSS-RR + 1spp PT 입력 분포 불일치
  (Phase 4 자체가 만든 문제 아님, Phase 12 NRD residual 에서 해결)

**의존성**: Phase 3 (smooth Î 사용)

**Phase 4 정리 사항**:
- R_min=1 시도 → 유리 검정/벽 firefly → R_min=4 로 안전 baseline
- Phase 7 후 reservoir reuse 가 Tier 3 픽셀 보충하면 R_min=1 가능
- DLSS-RR 지글거림은 본질적 한계 — Phase 8/11/12 에서 점진적 해결
- 디버그 시각화 (bounce count heatmap) 는 Phase 15 ablation 단계로 이월

---

### Phase 5 ✅ COMPLETED — Dynamic Frame Time Compensation (PASS 2 완성)

**완료 commit**: `69624ac` (DFTC infra) → `81afbba` (F3 검증 토글) → `128e885` (R_max clamp 하한 R_min) → `bb5ba6e` (씬 3 Fresnel 쇼케이스 cherry-pick)

**최종 산출물**:
- App 에 TIMESTAMP query 인프라:
  - `m_timestampHeap` (TIMESTAMP query, 2×3 슬롯 ring)
  - `m_timestampReadback` (readback buffer)
  - `m_timestampFreq = CmdQueue::GetTimestampFrequency()`
- Render 루프에 EndQuery begin/end + ResolveQueryData + 3 frame lag readback
- EMA (`m_gpuMsEMA = 0.9·prev + 0.1·current`) → spike 무시
- R_max 동적 조절: `clamp(round(8 × target_ms/T_gpu), R_min=4, 8)`
- F3 토글: `m_dftcTargetMs` 16.0 ↔ 1.0 (강제 trigger 검증)
- 60 frame 마다 GPU ms + R_max + target ms 콘솔 로그
- 씬 3 (Fresnel Showcase) 신규 — 7 instance + grazing camera, fresnel branch port

**검증 결과**:
- ✅ Timestamp freq = 1GHz (NVIDIA 표준)
- ✅ EMA 수렴 (초기 16ms → ~2.5ms in ~60 frames)
- ✅ 씬별 GPU 시간 차등 측정 (Outdoor 2.2 / Indoor 2.6 / Showcase 3.15 / Fresnel ~?)
- ✅ R_max stable 8 (현 부하 < 16ms target → compensation > 1 → clamp 8) — 정상
- ✅ F3 토글 강제 trigger 검증 (target 1ms → R_max 즉시 4 로 감소)
- ✅ Scene 3 추가로 GPU 부하 ~2× 증가 시도 — 여전히 budget 한참 아래

**의존성**: Phase 4 (cb.rMax 사용처)

**Phase 5 정리 사항**:
- F3 토글은 Phase 15 ablation 으로 자연 승계 (제거하지 않음)
- 씬 3 cherry-pick 은 Phase 5 검증 도구 + 향후 evaluation 씬
- R_max clamp 하한을 R_min(=4) 으로 보장 → lerp(R_min, R_max, Î) 순서 유지
- Glass 어두움은 Fresnel grazing 물리 + DLSS-RR 한계, Phase 12 NRD 에서 해결

---


### Phase 6 ✅ COMPLETED — Tier 기반 분기 (PHTR 통합)

**완료 커밋**: (이 commit) — CB tier 필드 + F4 토글 시각화

**최종 산출물**:
- `Common.h` SceneCB: `tierLow=0.30`, `tierHigh=0.70` 추가 (336→352 bytes)
- `Raytracing.hlsl`: CB 매칭 (Phase 6 시점 RT 분기 미사용, Phase 7 reservoir reuse 가 참조)
- `ImportanceVisualize.hlsl`: `showTier`/`tierLow`/`tierHigh` 필드 + RGB Tier 색 분기
  - Tier 1 (Î > 0.70) = 빨강 (full PT, reuse 금지)
  - Tier 2 (0.30 < Î ≤ 0.70) = 초록 (partial reuse 예정)
  - Tier 3 (Î ≤ 0.30) = 파랑 (aggressive reuse 예정)
- `ImportanceVisualizePass`: `SetTierMode(on, low, high)` API, VisCB 확장
- `App.cpp`:
  - F4 키 → `m_tierDebug` 토글, `m_importanceVis.SetTierMode(...)` 호출
  - `UpdateSceneCB` 에서 `cb.tierLow/tierHigh = k_tierLow/k_tierHigh` 채움
  - `k_tierLow=0.30`, `k_tierHigh=0.70` constexpr (RT 셰이더 ↔ Vis 동기화)
- F4 + F1 동시 ON 으로 화면 표시 (F1=Importance 시각화 활성, F4=Tier 색 모드)

**검증**:
- F1 + F4 ON → 씬 1/2/3 에서 모서리/하이라이트 = 빨강, flat = 파랑
- F4 OFF → 기존 heatmap 복귀

**Phase 6 정리 사항**:
- Threshold 상수는 `App::k_tierLow/k_tierHigh` 단일 출처 (RT CB ↔ Vis API 둘 다 같은 값)
- RT 셰이더에서 실제 tier 분기 동작은 Phase 7 (reservoir 도입 시) 활성

**의존성**: Phase 4 (importance SRV t5)

---

### Phase 7 — Motion Vector Radiance Reuse (Tier 2 전용)

**REVERT NOTE**: 원 Phase 7 안 (`52f9873`~`090c5a4` 4 커밋) 은 ReSTIR-style BRDF direction reservoir 였으나 사용자 의도와 무관 + estimator bias 다수 도입으로 **revert** (commits `9e06639`, `a33c8d9`, `5520921`, `c4179ff`). 페이즈는 닫지 않고 재설계.

**목표**: Tier 2 픽셀에서 prev frame 의 radiance 를 motion vector reprojection 으로 직접 복사.
**Per-pixel tier exclusivity 원칙**: ray-shot 픽셀은 reuse/denoise 절대 안 됨, reuse 픽셀은 ray 안 쏨, denoise 픽셀은 reuse 안 됨.

**Estimator 정합성**: prev frame radiance 는 그 frame 의 unbiased PT 결과. mv 로 동일 surface point 가리키면 estimator 일관성 유지. RIS/reservoir/statistical resampling 일체 사용 안 함.

**산출물**:
- `m_radianceHistory` (RGBA16F render-res) — prev frame PT 결과 보관
- `m_depthHistory`, `m_normalHistory` — prev frame GBuffer (validity 검사용)
- `m_reusedRadiance` (RGBA16F render-res) — Tier 2 reuse 결과
- `m_reuseValidMask` (R8_UNORM render-res) — 1=valid, 0=invalid (Phase 9 가 Tier 3 강등)
- `shaders/MotionVectorReuse.hlsl`:
  ```hlsl
  if (tier != 2) { validMask = 0; return; }
  float2 prevUV = (idx + 0.5 - mv) / dim;
  bool valid = |currDepth - prevDepth| / currDepth < 0.05
            && dot(currNormal, prevNormal) > 0.95
            && length(mv) < maxMV_threshold
            && prevUV in [0, 1];
  if (valid) reusedRadiance[idx] = bilinearSample(g_radianceHistory, prevUV);
  ```
- `App.cpp`:
  - PT 완료 후 GBuffer + radiance 를 history 버퍼에 복사 (다음 frame 입력)
  - dispatch Phase 7 compute (Tier 2 만)
  - F5 토글 (reuse on/off)
  - Phase 12 wiring 전엔 reuse 결과 미사용 (F6 디버그 시각화로 검증)

**검증**:
- F6 디버그: Tier 2 valid = 정상 색, invalid = 빨강
- 정적 씬: Tier 2 영역 ~100% valid
- 카메라 회전: silhouette 따라 invalid 증가 → Phase 9 가 Tier 3 강등 처리

**의존성**: Phase 6 (tier 분류)

**알려진 한계**:
- prev frame GBuffer 복사 비용 (~0.3ms)
- Phase 12 composite 까지는 시각적 효과 없음 (architecture-only)

---

### Phase 8 — Tier 2 History Clamp (Salvi Stability)

**목표**: Phase 7 의 `m_reusedRadiance` 에 3×3 valid neighbor 기반 luminance clamp 적용. ghosting / firefly 누적 방지.

**원 안 (motion vector reprojection) 은 Phase 7 로 흡수됨.**

**산출물**:
- `shaders/ReuseClamp.hlsl`:
  ```hlsl
  // 3×3 ptRadiance 의 luminance mean/stddev 계산 (valid neighbor 만)
  // reusedRadiance[idx] luminance 를 [mean - 3σ, mean + 3σ] 로 clamp
  // 색은 luminance 비율로 scale
  ```
- 입력: `m_reusedRadiance` (Phase 7), `m_renderAccum` (현 PT), `m_reuseValidMask`
- 출력: clamped `m_reusedRadiance`

**검증**:
- 카메라 small motion 시 prev frame highlight ghost 가 neighbor stat 으로 clamp
- firefly 가 reuse 누적되는 현상 차단

**의존성**: Phase 7

---

### Phase 9 — Hole Mask 구축 + Firefly Reclassification (보수적)

**목표**: 디노이저가 채울 픽셀 집합 결정. exclusivity 유지하며 극단 outlier 만 강등.

**Hole mask 조건**:
```
holeMask[idx] = 1 if any of:
  - tier == 3 (low importance, denoiser fills by design)
  - tier == 2 && reuseValidMask[idx] == 0 (invalid reuse → demote)
  - tier == 1/2 && ptLuminance[idx] > k_extremeOutlier (safety net)
```

**산출물**:
- `m_holeMask` (R8_UNORM render-res)
- `shaders/HoleMask.hlsl`:
  - 입력: `g_importance` (tier 분류용), `m_reuseValidMask`, `m_renderAccum`
  - `k_extremeOutlier = 32.0` 글로벌 절대 임계 (보수적 시작, Phase 14 후 조정)
  - 정상 highlight (lum 5-10) 는 통과, 진짜 outlier 만 강등 (false positive < 0.1% 목표)

**검증**:
- 카메라 회전 시 silhouette 픽셀 holeMask=1 (Tier 2 invalid → 강등)
- 정상 highlight 는 holeMask=0 (false positive 방지)

**의존성**: Phase 7 (validMask), Phase 6 (tier)

---

### Phase 10 — Edge-aware A-trous Hole Inpainting (NRD pre-fill)

**목표**: NRD 에 dense input 보장. hole 픽셀 *만* valid neighbor 로부터 채움.
**핵심 제약**: hole 픽셀에만 write. Tier 1/2 valid 픽셀 read-only (exclusivity 준수).

**산출물**:
- `shaders/HoleFill_ATrous.hlsl` — 4-5 iteration, stride 1/2/4/8(/16)
  ```hlsl
  // iteration k, stride = 1 << k
  if (holeMask[idx] == 0) { out[idx] = in[idx]; return; }
  // hole 픽셀: valid neighbor 가중 평균
  for (n in 5×5 with stride):
      if (holeMask[n] == 1) continue;             // hole neighbor 무시
      w_depth  = exp(-|z - z_n|² / σ_z);
      w_normal = pow(max(0, dot(N, N_n)), σ_n);
      w_lum    = neighbor_lum < percentile_99 × 5 ? 1 : 0;  // firefly 차단
      accum += w · in[n]; wSum += w;
  out[idx] = wSum > 0 ? accum / wSum : 0;
  ```
- 입력: Tier 1 PT + Tier 2 clamped reuse + holeMask
- 출력: `m_filledRadiance` — 모든 픽셀 dense
- ping-pong 2 버퍼 (iteration swap)
- 비용: ~1.0-1.5ms render-res

**검증**:
- F1 으로 holeMask 분포 확인 후 A-trous 출력 dense
- edge 보존 (depth/normal weight 가 boundary 차단)
- firefly 가 hole 로 새지 않음 (luminance weight)

**의존성**: Phase 9

---

### Phase 11 — DELETED

원 안: "Frequency Fusion (bilateral hi/lo)".
**삭제 사유**: NRD + DLSS-RR 가 사실상 같은 역할 (spatial-temporal denoise + AI). 사용자 의도 (exclusivity + NRD/DLSS 유지) 와 중복. 원 산출물의 edge-aware 효과는 Phase 10 (A-trous fill) 의 weight 함수로 흡수.

---

### Phase V1 — Volumetric Ray Marching 인프라

**근거**: PHTR § "Volumetric을 low-frequency long reuse 허용"  
> 인간이 volumetric detail에 둔감 + 자체적으로 고주파 변화 적음 → 강한 temporal reuse가 가장 효과적인 영역. 졸업작품 차별점으로도 유효.

**목표**: primary ray 경로상 homogeneous fog 누적 — `m_volumetric` (RGB=in-scatter, A=transmittance) 출력

**산출물**:
- `shaders/Volumetric.hlsl` — compute shader (RT 셰이더 아닌 별도 compute 패스로 분리)
  - 입력: `g_depth` (primary hit distance), 카메라 정보, 광원 정보, TLAS (shadow ray용 SRV)
  - 출력: `m_volumetric` (RGBA16F, render-res)
- Constants (씬별 조정):
  - `cb.fogSigmaT` — extinction coefficient (예: 0.05 / unit length)
  - `cb.fogSigmaS` — scattering = sigmaT × albedo
  - `cb.fogAnisotropy g` — Henyey-Greenstein anisotropy ([-1, 1], 0=isotropic, 양수=forward)
- 알고리즘 (per pixel):
  ```hlsl
  float tMax = primary hit distance from g_depth;
  uint N = 32;  // sample count
  float3 inScatter = 0;
  float  T = 1.0;  // accumulated transmittance
  float stratifiedJitter = HaltonSequence(pixelHash, frameIdx);
  for (uint i = 0; i < N; ++i) {
      float t = (i + stratifiedJitter) / N * tMax;
      float3 p = camPos + rayDir * t;
      float dt = tMax / N;
      // 광원별 in-scatter
      for each light L:
          shadow = TraceShadowRay(p, normalize(L.pos - p));
          attenL = exp(-sigmaT * dist(p, L.pos));  // light로 가는 길의 transmittance
          cosTheta = dot(rayDir, normalize(L.pos - p));
          phase = HGPhase(cosTheta, g);
          inScatter += T * sigmaS * phase * L.color * L.intensity * shadow * attenL * dt;
      T *= exp(-sigmaT * dt);
  }
  m_volumetric[idx] = float4(inScatter, T);
  ```
- 씬별 fog 파라미터 (scene preset):
  - Scene 0 (outdoor): sigmaT=0.03, g=0.4 (mie-like forward scatter)
  - Scene 1 (indoor): sigmaT=0.01 (거의 없음)
  - Scene 2 (PBR showcase): sigmaT=0.08, g=0.3 (시각화를 위해 진하게)
- 새 클래스 `VolumetricPass.{h,cpp}` (Denoiser 패턴)

**검증**:
- 어두운 씬에서 광원 주변에 god ray (volumetric shaft) 표시
- 카메라가 광원 정면을 바라볼 때 in-scatter halo (HG g>0 효과)
- transmittance가 거리에 따라 지수 감쇠

**비용 예상**: 1.5~3ms @ 854×480 (광원 2개 기준)

**의존성**: Phase 5 (Adaptive Ray) 완료 후 권장 (primary hit distance 확보됨). Phase 6~11과 병렬 진행 가능 — surface와 독립된 track.

---

### Phase V2 — Volumetric Long Temporal Reuse (PHTR 핵심)

**근거**: volumetric은 고주파 변화가 적고 인간이 둔감 → surface(N≤64) 대비 훨씬 강한 reuse(N≤128) 가능 → 노이즈 시각적 0 근접

**목표**: surface 보다 강한 EMA + 완화된 reprojection confidence로 volumetric 안정화

**산출물**:
- `m_volumetricHistory` (RGBA16F, render-res)
- `shaders/VolumetricReproject.hlsl` — compute
  ```hlsl
  float2 prevUV = currUV + motionVec / dim;
  float4 hist = SampleBilinear(m_volumetricHistory, prevUV);
  float4 curr = m_volumetric[idx];
  
  // 완화된 confidence — volumetric은 위치 정밀도 덜 중요
  float depthRatio = abs(z_curr - z_reproj) / z_curr;
  bool valid = (depthRatio < 0.15);  // surface는 0.05, vol은 0.15
  //  normal 일치는 검사 안 함 (volumetric은 surface normal 없음)
  
  // EMA: N capped at 128 (surface 64 의 2배)
  if (!valid) {
      out = curr;  // history reject
      N = 1;
  } else {
      uint N = min(hist.N + 1, 128);
      float alpha = 1.0 / float(N);
      out = lerp(hist, curr, alpha);
  }
  m_volumetricStable[idx] = out;
  ```
- N count는 별도 R8 채널 또는 history alpha 채널에 packed
- 출력: `m_volumetricStable` (RGBA16F)

**검증**:
- 정적 씬: 초기 ~16프레임에 volumetric noise 거의 사라짐
- 카메라 회전: god ray 위치 안정적으로 따라옴 (low-freq라 약간의 misalignment 허용)
- Reset (씬 전환): 1프레임에 history 무효화

**비용 예상**: < 0.3ms (단일 compute 패스, bilinear sample + EMA)

**의존성**: Phase V1 + Phase 8 (reprojection 인프라 패턴 동일)

---

### Phase V3 — Volumetric Composite 통합

**목표**: stabilized volumetric을 surface denoised 결과와 합성

**산출물**:
- `shaders/Composite.hlsl` (Phase 13에서 작성 예정) 에 volumetric 합성 추가:
  ```hlsl
  float3 surface = denoisedDiff * diffAlbedo
                 + denoisedSpec * specAlbedo
                 + emission;
  float4 vol = m_volumetricStable[idx];   // rgb=in-scatter, a=transmittance
  float3 final = surface * vol.a + vol.rgb;
  ```
- DLSS-RR Color 입력 = `final`
- 디버그 모드 확장:
  - `debugMode=13` → volumetric 단독 표시 (in-scatter)
  - `debugMode=14` → transmittance map (grayscale, 어두울수록 fog 짙음)
- 키 바인딩: `Shift+F` → fog ON/OFF 토글 (논문 비교용)

**검증**:
- god ray가 표면 lighting 위에 자연스럽게 합성
- 광원 근처: 흰색 fog halo
- 광원 멀리: surface 그대로 (transmittance≈1)
- Shift+F off 시 기존 결과와 동일

**비용 예상**: ~0.1ms (composite는 single texture sample 추가만)

**의존성**: Phase V2 + Phase 13 (composite 셰이더 작성 시점)

---

### Phase 12 — NRD Denoise + Per-tier Exclusive Composite

**목표**: NRD 로 hole-filled radiance 의 시간적 안정화 + per-tier exclusivity 강제 composite.

**핵심 제약**: composite 단계가 exclusivity 의 final enforcement 지점.
- Tier 1 픽셀: PT 결과만 사용 (NRD 출력 무시)
- Tier 2 valid: reuse 결과만 사용 (NRD 출력 무시)
- Tier 3 + invalid Tier 2: NRD 출력 사용 (PT 미사용)

**산출물**:

A. **NRD 디노이즈** (기존 `NRDDenoiser` 재사용)
- 입력: `m_filledRadiance` (Phase 10 dense output) → lobe 분리 (diff/spec)
- NRD RelaxSettings: 보수적 설정 (frameNum ~16 등)
- 출력: `m_nrdDenoised` (diff + spec, dense)

B. **Per-tier Composite** (`shaders/Composite.hlsl`):
  ```hlsl
  if (tier == 1)
      out[idx] = ptRadiance[idx];           // ray-shot only
  else if (tier == 2 && reuseValid[idx])
      out[idx] = reusedRadiance[idx];        // reuse only
  else
      out[idx] = nrdDenoised[idx];           // denoise only
  // emission 은 위 셋 중 어디서든 가산
  ```
- 입력: `m_renderAccum` (Tier 1 PT), `m_reusedRadiance` (Phase 8 clamped), `m_nrdDenoised`, `m_reuseValidMask`, `m_holeMask`, `g_importance`
- 출력: `m_compositedRadiance` (dense, exclusivity 강제)
- 디버그 모드: tier 별 어떤 source 가 선택됐는지 색으로 표시 (Tier 1=빨강, 2=초록, 3=파랑)

**검증**:
- composite 출력에 Tier 1 픽셀의 PT 값이 그대로 보존됨 (NRD 영향 0)
- Tier 3 영역이 NRD 로 부드럽게 채워짐
- exclusivity 단위 테스트: 같은 픽셀이 두 source 에서 동시에 값 받지 않음

**의존성**: Phase 10 (filled dense), 기존 NRD Init

---

### Phase 13 — DLSS-RR 연결

**목표**: composite 결과를 DLSS-RR Color 입력으로 연결.
**DLSS-RR 는 디노이저가 아닌 업스케일러로 취급** (사용자 명시) — 모든 픽셀 통과, exclusivity 예외.

**산출물**:
- DLSS-RR `Color` 입력 = `m_compositedRadiance` (Phase 12 출력)
- 기존 DLSS-RR Init 그대로 사용 (RR mode, render → display)
- albedo demodulation 은 DLSS-RR 가 내부 처리
- 출력: backbuffer (display res)

**검증**:
- 최종 화면이 HPAR pipeline 결과로 출력됨
- U 키 토글: HPAR composite ON/OFF 비교 (off 시 기존 1spp PT + DLSS-RR)
- "기존 DLSS-RR 단독 + paper crumpled 노이즈" vs "HPAR composite + DLSS-RR" 비교

**의존성**: Phase 12

---

### Phase 14 — Metrics 수집 + 디버그 UI

**목표**: LPIPS / Temporal Stability / SSIM 자동 수집

**산출물**:
- `src/MetricsCollector.{h,cpp}`:
  - LPIPS: Python 헬퍼 (off-line, 저장된 reference vs current 비교)
  - Temporal Stability: `mean(|frame_t - frame_{t-1}|)` GPU compute
  - SSIM: 간단한 11×11 Gaussian window 구현
- 키바인딩: `T` (트레이스 시작/종료), `Y` (마지막 N프레임 평균±σ 출력)
- CSV 출력: `metrics_<sceneID>_<config>.csv`

**검증**:
- 3개 씬 × 3개 구성(uniform 1spp, variance, ours) × 100프레임 평균 결과
- LPIPS reference: 1000spp offline render (별도 1회 생성)

**의존성**: Phase 13

---

### Phase 15 — Ablation Study 자동화

**목표**: 가중치 / γ / threshold ablation 표 자동 생성

**산출물**:
- 키 바인딩: `Shift+1~9` → 가중치 프리셋 전환
- Ablation 모드: `M 키` → 자동으로 9개 조합 ×N프레임 측정 후 CSV 저장
- 조합:
  - 가중치: `{w_e: 0.2/0.4/0.6}` × `{w_d: 0.1/0.25/0.4}`
  - γ: `{1.0, 1.5, 2.0, 3.0}`
  - T항 ON/OFF (검증: 본 구조에서 OFF가 더 좋아야 함)
- 결과 표 자동 생성 스크립트: `scripts/ablation_table.py`

**검증**:
- 9~12개 셀의 결과 표 CSV 출력
- 우리 기본 설정이 다른 ablation보다 LPIPS 우수해야 함

**의존성**: Phase 14

---

### Phase 16 — User Study 자료 준비

**목표**: 2AFC 비교 영상 + 정적 이미지 세트

**산출물**:
- `scripts/record_session.py` — 카메라 경로 ↔ 5초 영상 자동 녹화
- 3씬 × 3구성 × 2초/씬 → MP4 출력
- 정적 비교 이미지: 각 씬에서 critical view 5개
- Google Forms 템플릿 (별도 문서)

**검증**:
- 녹화 영상 framerate stable (30fps)
- 같은 카메라 경로에서 method만 다름

**의존성**: Phase 15

---

## 4. Phase 의존 그래프

```
[Surface Track]
Phase 1 (E+D)
  └─→ Phase 2 (정규화+S+M+V)
        └─→ Phase 3 (EMA)
              ├─→ Phase 4 (Adaptive Ray) ─────────────┐
              │     ├─→ Phase 5 (DFTC)                │
              │     └─→ Phase 6 (Tier 분류)            │
              │           └─→ Phase 7 (Reservoir)     │
              └─→ Phase 10 (Adaptive A-trous, Î 사용) │
                                                       │
Phase 4 ─→ Phase 8 (Temporal Reprojection) ←──────────┤
              └─→ Phase 9 (Hole + Firefly)             │
                    └─→ Phase 10 (Adaptive A-trous)    │
                          └─→ Phase 11 (Freq Fusion)   │
                                └─→ Phase 12 (NRD)     │
                                                       │
[Volumetric Track — parallel]                          │
Phase 5 ─→ Phase V1 (Vol RayMarch) ←───────────────────┘
              └─→ Phase V2 (Long Reuse, needs Phase 8 패턴)
                    └─→ Phase V3 (Composite 통합)
                                       ↓
[Composite & Output]                   ↓
Phase 12 + Phase V3 ────→ Phase 13 (Composite + DLSS)
                                ↓
                          Phase 14 (Metrics)
                                ↓
                          Phase 15 (Ablation, fog ON/OFF 포함)
                                ↓
                          Phase 16 (User Study)
```

총 **19 Phase** (Surface 16 + Volumetric 3). 1 Phase = 1 세션의 ~70%. 빌드 검증 + commit + 다음 세션 인계 메모 시간이 나머지 30%.

**병렬 진행 가능**: Surface Phase 6~11 작업 중 Volumetric V1/V2 를 별도 세션으로 끼워넣을 수 있음. V3 는 Phase 13 의존이라 반드시 후순위.

**추천 순서** (가장 자연스러운 진행):
```
1 → 2 → 3 → 4 → 5 → V1 → 6 → 7 → 8 → V2 → 9 → 10 → 11 → 12 → V3+13 → 14 → 15 → 16
```

---

## 5. 핵심 결정 사항 (기록)

### T항 분리 원칙 (Notion 문서 7장)
> Importance function은 **순수 perceptual attention**만 담당.  
> Temporal instability(`|C_t - C_{t-1}|`)는 artifact 측정값이므로 PASS 3에서 처리.  
> → feedback loop, camera motion 과민반응, noise 자체에 민감해지는 문제 회피.

### Percentile_99 vs max() 정규화
> max() 사용 시 firefly 단일 픽셀이 전체 스케일을 왜곡.  
> percentile_99 사용 → outlier robust.  
> Reinhard 2002 톤매핑에서 검증된 기법.

### V항 multiplier 처리
> Semantic saliency는 다른 항들과 스케일/의미가 다름.  
> 선형 합산 대신 `I·(1 + w_v·V)` multiplier로 처리.  
> V=0이면 I_final = I (semantic 없을 때 영향 없음).

### Specular `(1-r)²·clamp(H, 0, H_99)`
> raw H 사용 시 firefly에서 폭주.  
> H_99 clamp로 stable.  
> `(1-r)²`는 roughness가 낮을수록 importance 증가 (사람이 specular highlight에 집중).

### 상대적 motion `|v - v_mean_neighborhood|`
> 절대 `sqrt(vx²+vy²)` 사용 시 카메라 패닝이 화면 전체 importance 폭증시킴.  
> 주변 대비 튀는 motion만 의미 있음.

### 가중치 초기값
> `w_e=0.40, w_d=0.25, w_s=0.20, w_m=0.15`  
> Daly 1993 HVS CSF + Mannos & Sakrison 1974 근거.  
> Phase 15 ablation으로 검증/조정.

### Volumetric을 별도 track으로 처리
> PHTR § "Volumetric을 low-frequency long reuse 허용":  
> 인간이 volumetric detail에 둔감 + 자체적으로 고주파 변화 적음.  
> Surface(N≤64) 와 별도 EMA(N≤128) + 완화된 reprojection confidence(depth 15%, no normal).  
> 같은 importance map 사용하지 않음 — volumetric은 표면이 아니라 매질이므로 attention 분포가 다름.  
> Composite 시 `final = surface · T + in-scatter` 합성.

---

## 6. 측정 지표 우선순위

| 지표 | 역할 | 비고 |
|------|------|------|
| **LPIPS** | Primary | Zhang 2018 — perceptual quality |
| Temporal Stability | 핵심 보조 | flickering 측정 (Salvi 2016 컨벤션) |
| GPU ms | 성능 검증 | 16ms target (60fps) |
| SSIM | 보조 | 구조적 유사도 |
| PSNR | 참고용 | HVS와 상관 낮음 (Zhang 2018) |
| User Study | 체감 검증 | 2AFC 평가 (n≥30 권장) |

---

## 7. 비교 baseline

| Method | 설명 | 위치 |
|--------|------|------|
| **Uniform 1 spp** | 모든 픽셀 동일 PT 1spp | 동일 코드, importance=1.0 고정 |
| **Variance Adaptive** | variance 기반 ray 분배 (기존 기법) | 별도 분기 (Phase 14에서 토글) |
| **Perceptual Adaptive (Ours)** | 본 구조 전체 | 기본 |
| Pure DLSS-RR | path tracer 1spp + DLSS-RR만 | 별도 토글 (master branch 비교) |
| NRD-only | PT + NRD denoise (no importance) | 별도 토글 |

---

## 8. 참고 문헌 (출처 표시 필수)

- Itti, Koch & Niebur (1998) — Saliency-Based Visual Attention
- Daly (1993) — Visible Differences Predictor (HVS CSF)
- Mannos & Sakrison (1974) — Visual Fidelity Criterion
- Reinhard et al. (2002) — Photographic Tone Reproduction (percentile)
- Salvi (2016) — Temporal Supersampling (history clamp)
- Bitterli et al. (2020) — ReSTIR (reservoir reuse)
- Schied et al. (2017) — SVGF
- Hasselgren et al. (2020) — NTASD (temporal adaptive sampling)
- Zhang et al. (2018) — LPIPS
- Rousselle et al. (2012) — Adaptive Rendering w/ Weighted Local Regression
- Yarbus (1967) — Eye Movements and Vision

---

## 9. 위험 요소 & 완화 방안

| 위험 | 영향 | 완화 |
|------|------|------|
| Tier 1 영역이 너무 커서 budget 폭주 | 60fps 미달 | Phase 5 DFTC가 R_total 자동 감소 |
| Temporal reuse → ghosting | Tier 1 영역 끈적함 | Phase 8 history clamp + Tier 1 reuse 금지 |
| Reservoir 구현 복잡도 | 일정 지연 | Phase 7 단순 1-sample reservoir로 시작, ReSTIR full은 차후 |
| Percentile_99 계산 비용 | per-frame overhead | 256-bin histogram 1-pass, ~0.2ms 예상 |
| NRD 통합 미완 상태에서 진행 | 의존성 충돌 | Phase 12에서 NRD wiring 마무리. 그 전엔 NRD off로 진행 가능 |
| Semantic NN (V) cost | 별도 추론 부담 | V는 stub으로 시작. Phase 16 이후 선택적 추가 |
| Volumetric 32-sample × 광원 ray 비용 | 1.5~3ms 추가 | sample N 동적 조절 (Tier-1 영역에서만 32, 그 외 16). 또는 1/2 해상도로 |
| Volumetric long reuse → fog 지연 lag | 광원 갑자기 켜질 때 god ray 천천히 나타남 | Phase V2 confidence 에 luminance change detection 추가 (옵션) |

---

## 10. 산출물 체크리스트 (졸업 작품 기준)

- [ ] Phase 1~13 모두 구현 (paper의 PASS 1/2/3 완성)
- [ ] Phase V1~V3 구현 (Volumetric long reuse — PHTR 차별점)
- [ ] Phase 14 metrics 수집 시스템 동작
- [ ] Phase 15 ablation 표 (CSV) 생성됨 — fog ON/OFF 셀 포함
- [ ] Phase 16 사용자 평가 영상 + Forms 준비
- [ ] LPIPS 결과 표 (3씬 × 3method × 평균±σ)
- [ ] Temporal Stability 표
- [ ] GPU ms 표 (각 PASS별 + 전체, volumetric track 분리)
- [ ] Figure 1: teaser (Tier 시각화 + 결과 + god ray)
- [ ] Figure 2: Pipeline 다이어그램 (surface + volumetric 병렬 track)
- [ ] Figure 3: 비교 (uniform / variance / ours)
- [ ] Figure 4: GPU 타이밍 bar chart
- [ ] Figure 5: User study 결과
- [ ] Figure 6: Volumetric long reuse ablation (N=16 vs 64 vs 128, fog noise 비교)
- [ ] 논문 본문 (10~15p) — 이 기획안의 §1, 2를 introduction/method로 확장
